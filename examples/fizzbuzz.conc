
0 while dup 1024 < do
    dup 3 % 0 = if
        "Fizz" print
    end

    dup 5 % 0 = if
        "Buzz" print
    end

    dup 3 %
    over 5 %
    * 0 !=     # (x % 3) * (x % 5) != 0 -> x isn't multiple of 3 or 5
    if
        dup debug
    end

    " " print
    
    1 +
end drop